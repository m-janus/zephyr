common:
  tags: drivers led
  build_only: true
  platform_allow: native_posix
tests:
  drivers.led.build:
    min_ram: 32
  drivers.led.i2c.build:
    tags: led_lp50xx led_lp5009 led_lp5030
    extra_args: CONFIG_I2C=y CONFIG_LP50XX=y
